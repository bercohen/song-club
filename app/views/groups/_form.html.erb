<%= form_for(group) do |f| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
 <%if group&.id%>
 <button class="button-primary">
    <%= link_to("Add concert",new_group_concert_path(group)) %>
</button>
<%end%>

  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
